services:
  zashboard:
    ports:
      - 9090:80
    image: ghcr.io/zephyruso/zashboard:latest
    network_mode: bridge
    environment:
      - PUID=3000
      - PGID=3000
      - TZ=Asia/Shanghai
    restart: unless-stopped
